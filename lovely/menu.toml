[manifest]
version = "1.1.1"
dump_lua = true
priority = 0

[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
st:setFgDraw(function(self)
'''
position = "after"
payload = '''
if maininput:down("f9") then
	self.editMenu = true
end

if self.chimaera then
	self.menuMusicManager:stop()
	
	--[[cs = bs.load('BattleState')
	cs.bg = self.bg
	cs:init({{'chimaeraBoss', { ogx = 467, ogy = 153 }}}, {"The walls shift...",
		"A gust of wind rattles the room.",
		"Distant echoes.",
		"The ground trembles slightly.",
		"Shadows dance across the walls.",
		"Time seems to slow down.",
		"A faint rumble echoes around.",
		"The air feels heavy.",
		"Silence fills the space.",
		"A eerie calm settles.",
		"Cranky wants cornbread",
		"..."})]]
		
	cs = bs.load('WorldState')
	self.bg.delete = true
	cs:init('Mods/ExtraStuff/rooms/EraChimaeraRoom.json')
end

local function shuffle(t)
	for i = #t, 2, -1 do
		local j = love.math.random(i)
		t[i], t[j] = t[j], t[i]
	end
end

if self.editMenu then
	self.editMenu = imgui.Begin("Edit Menu", true)
	
	if not self.oldMenu then
		imguiextra.DrawReorderableRadialList(self.mainMenu, "mainMenu", {imgui.GetContentRegionAvail().x, 24})

		if imgui.Button("Reset") then
			self.mainMenu:restoreHidden(extraDefaultSave.disabledMainOptions)
			extrasavedata.disabledMainOptions = extraDefaultSave.disabledMainOptions
			self.mainMenu:reorderToIds(extraDefaultSave.mainMenuList)
		end

		imgui.SameLine()

		if imgui.Button("Randomize") then
			shuffle(extrasavedata.mainMenuList)
			self.mainMenu:reorderToIds(extrasavedata.mainMenuList)
		end

		self.mainMenu:saveList(self.mainMenu, "mainMenuList", "disabledMainOptions")
	else
		dpf.saveJson("savedata/extraMod.sav", extrasavedata)
	end
	imguiextra.LabeledSeparator("Customization", 1)
	if not extrasavedata.moreMMenuCustomization then
		extrasavedata.moreMMenuCustomization = helpers.copytable(extraDefaultSave.moreMMenuCustomization)
	end
	extrasavedata.oldMenu = helpers.InputBool("Demo Menu Style (No Reordering (ME LAZY))", extrasavedata.oldMenu or false)
	if extrasavedata.moreMMenuCustomization then
		extrasavedata.moreMMenuCustomization.enabled = helpers.InputBool("More Customization", (extrasavedata.moreMMenuCustomization.enabled or false))
		if extrasavedata.moreMMenuCustomization.enabled then
			imguiextra.LabeledSeparator("Logo", 1)
			local logos = {"Beatblock", "BtRaNaToWnFnaSoCSWtItbE",} --"BeatABlock"}
			if not extrasavedata.moreMMenuCustomization.logo then extrasavedata.moreMMenuCustomization.logo = "Beatblock" end
			if imgui.BeginCombo("##LogoCombo", extrasavedata.moreMMenuCustomization.logo) then
				for i, v in ipairs(logos) do
					local isSelected = (v == extrasavedata.moreMMenuCustomization.logo)
					if imgui.Selectable_Bool(v, isSelected) then
						extrasavedata.moreMMenuCustomization.logo = v
						if sprites.title[extrasavedata.moreMMenuCustomization.logo] then
							local i = sprites.title[extrasavedata.moreMMenuCustomization.logo]
							if extrasavedata.moreMMenuCustomization.coloredLogo and sprites.title[extrasavedata.moreMMenuCustomization.logo .. "_colored"] then
								i = sprites.title[extrasavedata.moreMMenuCustomization.logo .. "_colored"]
							end
							sprites.title.logo = i
						else
							extrasavedata.moreMMenuCustomization.logo = "Beatblock"
						end
					end
				end
				imgui.EndCombo()
			end
			extrasavedata.moreMMenuCustomization.coloredLogo = helpers.InputBool("Colored Logo (Shuv still applies)", extrasavedata.moreMMenuCustomization.coloredLogo or false)
			
			--local backgroundEntities = {"MenuBackground", "oldTitle", "CreditsBG", "cbackground", "TutorialBG", "ShinamonBG", "Rain", "HoleBackground", "TrumpetGirl", "Plasma", "SineWaves", "LawrenceBG", "DamoclismBG", "SpiralBG", "DecoAtomMap", "MonitorTheme"}
			local backgroundEntities = {"MenuBackground", "NoBG", "oldTitle", "CreditsBG", "cbackground", "TutorialBG", "ShinamonBG", "Rain", "HoleBackground", "PublicoBG", "Plasma", "SineWaves", "LawrenceBG", "DamoclismBG", "SpiralBG", "Player"}
			
			imguiextra.LabeledSeparator("Background Entity", 1)
			if imgui.BeginCombo("##BackgroundEntity", extrasavedata.moreMMenuCustomization.backgroundEntity) then
				for i, v in ipairs(backgroundEntities) do
					local isSelected = (v == extrasavedata.moreMMenuCustomization.backgroundEntity)
					if imgui.Selectable_Bool(v, isSelected) then
						extrasavedata.moreMMenuCustomization.backgroundEntity = v
						local param = em.init(v)
						self.bgparams = helpers.copytable(param)
						param.delete = true
					end
				end
				imgui.EndCombo()
			end
			if imgui.CollapsingHeader_TreeNodeFlags("Parameters") then
				if type(self.bgparams or nil) == "table" then
					imguiextra.drawTable(self.bgparams, {boolean = true, number = true, string = true}, {skipRender = true, skipUpdate = true, delete = true})
					local function extractValues(tbl, allowedTypes)
						local result = {}
						for k, v in pairs(tbl) do
							local t = type(v)
							if allowedTypes[t] then
								result[k] = v
							end
						end
						return result
					end
					extrasavedata.moreMMenuCustomization.bgParams = extractValues(self.bgparams, {boolean = true, number = true, string = true})
				else
					self.bgparams = helpers.copy(extrasavedata.moreMMenuCustomization.bgParams)
				end
				imgui.TextWrapped("If you crash your game after reloading. Go to savedata\\extraMod.sav")
				if imgui.Button("Clear Params") then
					self.clearBGParams = true
					extrasavedata.moreMMenuCustomization.bgParams = {}
				end
			end
			
			imguiextra.LabeledSeparator("Colors", 1)
			for i = 0, 7 do
				local color = extrasavedata.moreMMenuCustomization.palette[tostring(i)]
				color.r, color.g, color.b = helpers.imguiColor(tostring(i), color.r, color.g, color.b)
				shuv.pal[i] = helpers.copy(color)
			end
			if imgui.Button("Reset Colors") then
				shuv.resetPal()
				shuv.pal[2] = {r= 205, g=205, b=205}
				shuv.pal[3] = {r= 255, g=52, b=50}
				shuv.pal[4] = {r= 224, g=227, b=0}
				shuv.pal[5] = {r= 44, g=255, b=57}
				shuv.pal[6] = {r= 0, g=222, b=229}
				shuv.pal[7] = {r= 63, g=38, b=255}
				for i = 0, 7 do
					local color = shuv.pal[i]
					extrasavedata.moreMMenuCustomization.palette[tostring(i)] = helpers.copy(color)
				end
			end
		end
	end
	imgui.Separator()
	if imgui.Button("Reload") then
		if self.clearBGParams then
			extrasavedata.moreMMenuCustomization.bgParams = {}
		end
		cs = bs.load('Menu')
		self.menuMusicManager:clearOnBeatHooks()
		cs.menuMusicManager = self.menuMusicManager
		cs:init()
		cs.editMenu = true
	end
	
	imgui.End()
	
	if imgui.love.GetWantCaptureMouse() then
		love.mouse.setVisible(true)
	elseif savedata.options.game.customCursorInMenu and (savedata.options.game.cursorMode ~= "default") then
		love.mouse.setVisible(false)
	end
	
elseif savedata.options.game.customCursorInMenu and (savedata.options.game.cursorMode ~= "default") then
	love.mouse.setVisible(false)
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
self.mainMenu = em.init('RadialList',{allowInput = true, circleWidth = 72})
'''
position = "at"
payload = '''
self.oldMenu = extrasavedata.oldMenu
if self.oldMenu then
	self.mainMenu = em.init('OptionsList',{allowInput = true})
else
	self.mainMenu = em.init('RadialList',{allowInput = true, circleWidth = 72})
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
if project.event or (not project.release) then
'''
position = "at"
payload = '''
if not self.oldMenu and (project.event or (not project.release)) then
'''
match_indent = true
[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
self.mainMenu:addOption('credits',function()
'''
position = "before"
payload = '''
if not self.oldMenu then
'''
match_indent = true
[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
end, 17*5, nil, sprites.menu.credits)
'''
position = "after"
payload = '''
end
self.mainMenu:addOption('Mods', function()
	cs = bs.load('Mods')
	self.menuMusicManager:clearOnBeatHooks()
	cs.menuMusicManager = self.menuMusicManager
	cs:init()
end, 17 * 7, nil, sprites.menu.mods)
'''
match_indent = true
[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
self.mainMenu:setSelection(_G['mainMenuSelection'] or 1)
'''
position = "before"
payload = '''
for i = 1, #self.mainMenu.options.main do
	self.mainMenu.options.main[i].y = ((i-3) * 17) - (((#self.mainMenu.options.main/11)-1) * 17)
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
		savedata.seenIntroPopups = true
		sdfunc.save()
	end'''
position = "after"
payload = '''
if not self.oldMenu then
	self.mainMenu:reorderToIds(extrasavedata.mainMenuList)
	self.mainMenu:addHidden(extrasavedata.disabledMainOptions)

	if extrasavedata.randomizeMenuOnStart then
		for i = #extrasavedata.mainMenuList, 2, -1 do
			local j = love.math.random(i)
			extrasavedata.mainMenuList[i], extrasavedata.mainMenuList[j] = extrasavedata.mainMenuList[j], extrasavedata.mainMenuList[i]
		end
		self.mainMenu:reorderToIds(extrasavedata.mainMenuList)
	end
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
self.mainMenu:draw(self.x * project.res.cx, self.mainMenuY, 500)
'''
position = "at"
payload = '''
if self.oldMenu then
	self.mainMenu:draw(self.x * project.res.cx, 200)
else
	self.mainMenu:draw(self.x * project.res.cx, self.mainMenuY, 500)
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
--self.optionsMenu:addOption('optionsLanguage','language',optionsHeight*1)
'''
position = "at"
payload = '''
self.optionsMenu:addOption('optionsLanguage','language',optionsHeight*0)
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
self.bg = self.bg or em.init('MenuBackground')
'''
position = "at"
payload = '''
self.bgparams = {}
if extrasavedata.moreMMenuCustomization and extrasavedata.moreMMenuCustomization.enabled and em.entities[extrasavedata.moreMMenuCustomization.backgroundEntity] then
	shuv.resetPal()
	local palette = extrasavedata.moreMMenuCustomization.palette
	for i = 0, 7 do
		local key = tostring(i)
		shuv.pal[i] = { 
			r = palette[key].r,
			g = palette[key].g,
			b = palette[key].b
		}
	end
	self.bgparams = extrasavedata.moreMMenuCustomization.bgParams
	self.bg = self.bg or em.init(extrasavedata.moreMMenuCustomization.backgroundEntity,extrasavedata.moreMMenuCustomization.bgParams)
	if self.bg.onBeatHook and self.menuMusicManager then
		self.menuMusicManager:addOnBeatHook(self.bg.onBeatHook)
	end
	if extrasavedata.moreMMenuCustomization.logo and sprites.title[extrasavedata.moreMMenuCustomization.logo] then
		local i = sprites.title[extrasavedata.moreMMenuCustomization.logo]
		if extrasavedata.moreMMenuCustomization.coloredLogo and sprites.title[extrasavedata.moreMMenuCustomization.logo .. "_colored"] then
			i = sprites.title[extrasavedata.moreMMenuCustomization.logo .. "_colored"]
		end
		sprites.title.logo = i
	end
else
	self.bg = self.bg or em.init('MenuBackground')
	sprites.title.logo = sprites.title["Beatblock"]
end

if extrasavedata.spashText or false then
	self.splashtext = loc.get("extrastuffsplashtext_" .. math.random(0,loc.get("extrastuffsplashtext_num")))
	--self.splashtext = loc.get("extrastuffsplashtext_68")
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/Menu.lua"
pattern = '''
if self.tutorialPopup then
'''
position = "before"
payload = '''
if self.splashtext then
	local text = self.splashtext
	local font = fonts.main
	
	local baseWidth = font:getWidth("FUCKJUMPBEATS") --this is just default text width ok
	local textWidth = font:getWidth(text)
	
	local size = self.logoZoom + (baseWidth / textWidth)
	
	local x = project.res.cx + 130
	local y = 80 + sprites.title.logo:getHeight()/2 + self.logoY
	
	rtf:drawRich(text,x-1,y,font,size,size,0,500,"center",false,0,0,0,-15,0,0)
	rtf:drawRich(text,x+1,y,font,size,size,0,500,"center",false,0,0,0,-15,0,0)
	rtf:drawRich(text,x,y-1,font,size,size,0,500,"center",false,0,0,0,-15,0,0)
	rtf:drawRich(text,x,y+1,font,size,size,0,500,"center",false,0,0,0,-15,0,0)
	
	--love.graphics.print(text, x, y, math.rad(-15), size, size, textWidth/2, font:getHeight()/2)
	rtf:drawRich(text,x,y,font,size,size,1,500,"center",false,0,0,0,-15,0,0)
	color(0)
	love.graphics.setFont(fonts.digitalDisco)
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/AtomMap.lua"
pattern = '''
self.bg = self.bg or em.init("MenuBackground")
'''
position = "at"
payload = '''
self.bgparams = {}
if extrasavedata.moreMMenuCustomization and extrasavedata.moreMMenuCustomization.enabled and em.entities[extrasavedata.moreMMenuCustomization.backgroundEntity] then
	shuv.resetPal()
	local palette = extrasavedata.moreMMenuCustomization.palette
	for i = 0, 7 do
		local key = tostring(i)
		shuv.pal[i] = { 
			r = palette[key].r,
			g = palette[key].g,
			b = palette[key].b
		}
	end
	self.bgparams = extrasavedata.moreMMenuCustomization.bgParams
	self.bg = self.bg or em.init(extrasavedata.moreMMenuCustomization.backgroundEntity,extrasavedata.moreMMenuCustomization.bgParams)
	if self.bg.onBeatHook and self.menuMusicManager then
		self.menuMusicManager:addOnBeatHook(self.bg.onBeatHook)
	end
else
	self.bg = self.bg or em.init('MenuBackground')
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/AtomMap.lua"
pattern = '''
self.menuMusicManager = em.init("MenuMusicManager")
'''
position = "after"
payload = '''
	if self.bg.onBeatHook and self.menuMusicManager then
		self.menuMusicManager:addOnBeatHook(self.bg.onBeatHook)
	end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/AtomMap.lua"
pattern = '''
		shuv.drawWithPalette(function()
			love.graphics.draw(self.bgCanv)
		end, {
			[0] = { r = 255, g = 255, b = 255 },
			[1] = { r = 0, g = 0, b = 0 },
			[2] = { r = 205, g = 205, b = 205 },
			[3] = { r = 255, g = 52, b = 50 },
			[4] = { r = 224, g = 227, b = 0 },
			[5] = { r = 44, g = 255, b = 57 },
			[6] = { r = 0, g = 222, b = 229 },
			[7] = { r = 63, g = 38, b = 255 },
		})
'''
position = "after"
payload = '''
	if extrasavedata.moreMMenuCustomization and extrasavedata.moreMMenuCustomization.enabled then
	local palette = extrasavedata.moreMMenuCustomization.palette
	local pal = {}
	for i = 0, 7 do
		local key = tostring(i)
		pal[i] = { 
			r = palette[key].r,
			g = palette[key].g,
			b = palette[key].b
		}
	end
		shuv.drawWithPalette(function()
			love.graphics.draw(self.bgCanv)
		end, pal)
	end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "states/AtomMap.lua"
pattern = '''
shuv.pal[7] = { r = 63, g = 38, b = 255 }
'''
position = "after"
payload = '''
shuv.showBadColors = true
'''
match_indent = true

#[[patches]]
#[patches.pattern]
#target = "states/AtomMap.lua"
#pattern = '''
#love.graphics.setCanvas({ oldCanvas, stencil = true })
#'''
#position = "before"
#payload = '''
#love.graphics.setColor(shuv.pal[0].r/255 or 1,shuv.pal[0].g/255 or 1,shuv.pal[0].b/255 or 1,1)
#'''
#match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/damoclism/DamoclismBG.lua"
pattern = '''
	self.i = self.i + dt*self.spinSpeed*cs.rateMod
	self.crystalSpin = self.crystalSpin + self.crystalSpinSpeed*dt*cs.rateMod
	self.swordRingSpin = self.swordRingSpin + self.swordRingSpinSpeed*dt*cs.rateMod
'''
position = "at"
payload = '''
	self.i = self.i + dt*self.spinSpeed*(cs.rateMod or 1)
	self.crystalSpin = self.crystalSpin + self.crystalSpinSpeed*dt*(cs.rateMod or 1)
	self.swordRingSpin = self.swordRingSpin + self.swordRingSpinSpeed*dt*(cs.rateMod or 1)
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/BEATROCK/HoleBackground.lua"
pattern = '''
	self.spawnTimer = self.spawnTimer - (cs.cBeat - self.lastbeat)
	self.lastbeat = cs.cBeat
'''
position = "before"
payload = '''
	if not cs.cBeat or cs.noBeat then
		cs.cBeat = (cs.cBeat or 0) + dt/60
		cs.noBeat = true
	end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/tutorial/TutorialBG.lua"
pattern = '''
dt = dt*cs.rateMod
'''
position = "before"
payload = '''
if not cs.rateMod then
	cs.rateMod = 1
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/orchids/Plasma.lua"
pattern = '''
dt = dt*cs.rateMod
'''
position = "before"
payload = '''
if not cs.rateMod then
	cs.rateMod = 1
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/orchids/SineWaves.lua"
pattern = '''
dt = dt*cs.rateMod
'''
position = "before"
payload = '''
if not cs.rateMod then
	cs.rateMod = 1
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/rain/Rain.lua"
pattern = '''
dt = dt*cs.rateMod
'''
position = "before"
payload = '''
if not cs.rateMod then
	cs.rateMod = 1
	cs.noratemod = true
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/rain/Rain.lua"
pattern = '''
function Rain:draw()
'''
position = "after"
payload = '''
if cs.noratemod then
	self:drawBG()
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/lawrence/LawrenceBG.lua"
pattern = '''
prof.push("LawrenceBG update")
'''
position = "after"
payload = '''
if not cs.rateMod then
	cs.rateMod = 1
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/lawrence/LawrenceBG.lua"
pattern = '''
self.cumulativeAngle = self.cumulativeAngle + cs.p.angleDelta
'''
position = "at"
payload = '''
self.cumulativeAngle = self.cumulativeAngle + (cs.p and cs.p.angleDelta or 0)
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "obj/level/shinamon/ShinamonBG.lua"
pattern = '''
function ShinamonBG:update(dt)
'''
position = "after"
payload = '''
if not cs.rateMod then
	cs.rateMod = 1
end
'''
match_indent = true
